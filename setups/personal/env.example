# Claudicle — Personal Environment Variables
# Copy relevant lines to your ~/.zshrc or ~/.bashrc

# Required: Claudicle installation path
export CLAUDICLE_HOME="$HOME/.claudicle"

# Optional: Slack integration
# Create a Slack app at https://api.slack.com/apps with Socket Mode enabled
export SLACK_BOT_TOKEN="xoxb-your-bot-token"
export SLACK_APP_TOKEN="xapp-your-app-token"

# Optional: SMS integration (Telnyx)
export TELNYX_API_KEY="KEY..."
export TELNYX_PHONE_NUMBER="+1234567890"

# Optional: SMS integration (Twilio)
export TWILIO_ACCOUNT_SID="AC..."
export TWILIO_AUTH_TOKEN="..."
export TWILIO_PHONE_NUMBER="+1234567890"

# Optional: Soul identity (must match the name in soul/soul.md)
# export CLAUDICLE_SOUL_NAME="Claudius"        # Default: Claudius

# Optional: Override defaults
# export CLAUDICLE_CWD="$HOME/projects"       # Working directory for Claude
# export CLAUDICLE_TIMEOUT="180"               # Claude response timeout (seconds)
# export CLAUDICLE_TOOLS="Read,Glob,Grep,Bash,WebFetch"  # Allowed tools
